# Files/dirs listed here are preserved across sysupgrade, on top of the
# defaults in /lib/upgrade/keep.d/base (which already covers /etc/config,
# /etc/dropbear, /etc/crontabs, etc). One path per line.
/etc/caddy/
